# Diplom_2
# API автотесты для Stellar Burgers

## Задание 2: Автотесты для API

### Автотесты для проверки API сервиса Stellar Burgers

### Реализованные сценарии

Созданы автотесты, покрывающие основные эндпоинты API:

#### Создание пользователя (5 тестов)
- Создание уникального пользователя
- Попытка создания уже зарегистрированного пользователя  
- Создание пользователя без обязательных полей (email, password, name)

#### Логин пользователя (4 теста)
- Успешный вход под существующим пользователем
- Вход с неверным логином и паролем

#### Создание заказа (4 теста)
- Создание заказа с авторизацией и ингредиентами
- Создание заказа без авторизации (*баг приложения*)
- Создание заказа без ингредиентов
- Создание заказа с неверным хешем ингредиентов
**Примечание:** Тест "Создание заказа без авторизации" падает, так как API позволяет создавать заказы без авторизации, что противоречит документации (ожидается 401/403).

### Технологии и подходы

- **pytest** - фреймворк для тестирования
- **requests** - для HTTP запросов к API
- **allure-pytest** - для создания детальных отчетов
- **Page Object Pattern** адаптированный для API (API Client Pattern)
- **Параметризация** - для тестирования различных сценариев
- **Фикстуры** - для переиспользуемых данных и настроек
- **Изоляция тестов** - каждый тест создает и удаляет своего пользователя
- **Allure Steps** - аннотации для всех API запросов

### Структура проекта
Diplom_2/
├── tests/
│ ├── api/ # API клиенты с Allure steps
│ │ ├── init.py
│ │ ├── base_api.py # Базовый класс для API запросов
│ │ ├── user_api.py # API клиент пользователей
│ │ └── order_api.py # API клиент заказов
│ ├── init.py
│ ├── conftest.py # Фикстуры Pytest
│ ├── helpers.py # Вспомогательные функции
│ ├── test_data.py # Тестовые данные и константы
│ ├── test_user_api.py # Тесты создания пользователя
│ ├── test_user_login.py # Тесты авторизации
│ ├── test_order_api.py # Тесты создания заказов
│ └── urls.py # URL эндпоинтов API
├── .gitignore # Игнорируемые файлы
├── requirements.txt # Зависимости проекта
└── README.md # Документация

### Запуск тестов 
# Все тесты
pytest tests/

# С генерацией Allure отчета
pytest tests/ --alluredir=allure-results
allure serve allure-results

# Конкретные тесты
pytest tests/test_user_api.py
pytest tests/test_user_login.py  
pytest tests/test_order_api.py

# Результаты тестирования
Всего тестов: 13
Успешно: 12 (92.3%)
Провален: 1 (тест на соответствие документации)
Проваленный тест: test_create_order_without_auth
Причина: API возвращает 200 вместо ожидаемого 401 по документации.